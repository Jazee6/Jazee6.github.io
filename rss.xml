<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Hi! Jazee</title><description>Jazee&apos;s personal blog. Powered by Astro Blog Theme Blur.</description><link>https://jaze.top/</link><item><title>I built a chat app where every room is its own server (on Cloudflare&apos;s edge)</title><link>https://jaze.top/posts/building-chat-room-is-own-server/</link><guid isPermaLink="true">https://jaze.top/posts/building-chat-room-is-own-server/</guid><description>How Durable Objects let you treat each chat room as an isolated stateful microservice with its own database, WebSocket connections, and WebRTC coordination, all without a traditional backend.</description><pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate><content:encoded>Most real-time chat apps share the same architecture: a fleet of stateless API servers, a Redis pub/sub layer for WebSocket fanout, and a PostgreSQL database sitting behind a connection pooler. It works, but it&apos;s a lot of infrastructure for what is essentially &quot;user sends message, other users see message.&quot;

I wanted to see if I could build something different. The result is [web-chat](https://chat.jaze.top), a real-time text and voice calling application where every single chat room runs as its own isolated server, with its own database, on Cloudflare&apos;s edge network. No VMs, containers, or Redis involved. It runs on Durable Objects, D1, R2, and RealtimeKit.

Here&apos;s how it works and what I learned building it.

## The problem with stateless servers for stateful rooms

A chat room is inherently stateful. At any given moment, you need to know who&apos;s connected, what messages they&apos;ve sent, and whether they&apos;re in a voice call. The traditional approach spreads this state across multiple services: Redis for presence, a database for messages, and maybe another service for WebRTC signaling.

Each of these services introduces latency. A message from User A bounces through the API server, into Redis, back out to the WebSocket connection of User B. If your API server crashes, you lose track of who&apos;s connected. If Redis blips, messages get dropped.

What if the room itself could hold its own state?

## Each room as a Durable Object

Cloudflare Durable Objects solve this by giving you a stateful, single-threaded compute unit that can persist data locally. In my implementation, each chat room maps to exactly one Durable Object instance. When you create a room, the system generates a unique DO ID:

```typescript
const roomId = c.env.ROOM.newUniqueId();
```

When a user opens the chat page, the Hono router on the Worker looks up the room in D1 (the global relational database), finds the corresponding DO stub, and proxies the WebSocket upgrade directly to it:

```typescript
app.get(&apos;/room/:id/ws&apos;, authMiddleware, async (c) =&gt; {
  const roomId = c.req.param(&apos;id&apos;);
  const stub = c.env.ROOM.get(roomIdFromName);
  return stub.fetch(c.req.raw.url, c.req.raw);
});
```

Inside the Durable Object, all WebSocket connections live in a `Map&lt;WebSocket, WsSession&gt;`. When a user sends a message, the DO broadcasts it to every connected client. There&apos;s no pub/sub or message queue involved, just a loop over a Map.

```typescript
for (const [ws, session] of this.sessions) {
  ws.send(JSON.stringify({ type: &apos;message&apos;, data: message }));
}
```

This is dramatically simpler than the traditional architecture. The room handles its own presence, its own message routing, and its own persistence. If the DO gets evicted from memory (Cloudflare can hibernate idle objects), it rehydrates its session map from serialized WebSocket attachments on the next wake-up:

```typescript
constructor(ctx, env) {
  this.sessions = new Map();
  // WebSockets survive hibernation. Rehydrate sessions.
  for (const ws of this.ctx.getWebSockets()) {
    const session = ws.deserializeAttachment();
    this.sessions.set(ws, session);
  }
}
```

## Two databases, one ORM

The most interesting architectural decision was how to store messages. I could have put everything in D1 (Cloudflare&apos;s edge-distributed SQLite). But that would mean every message write goes over the network to D1, adding latency to the hot path.

Instead, I split the storage into two layers:

**D1** stores relational metadata: users, rooms, favorites, auth sessions. Things you need to query across (like &quot;all rooms a user has joined&quot; or &quot;favorite rooms&quot;).

**Durable SQLite** stores per-room messages. Each Room DO gets its own embedded SQLite database that lives right next to the compute handling that room&apos;s WebSocket connections.

Both use Drizzle ORM, but with separate configurations:

```typescript
// D1 config: users, rooms, auth
export default defineConfig({
  dialect: &apos;sqlite&apos;,
  driver: d1Http(migrationsFolder),
  schema: &apos;./server/src/lib/schema.ts&apos;,
  out: &apos;./server/drizzle/d1&apos;,
});

// Durable SQLite config: messages per room
export default defineConfig({
  dialect: &apos;sqlite&apos;,
  driver: durableSqlite(migrationsFolder),
  schema: &apos;./server/src/lib/do-schema.ts&apos;,
  out: &apos;./server/drizzle/room&apos;,
});
```

The trade-off is clear: I can&apos;t easily run cross-room queries like &quot;all messages by user X across all rooms.&quot; But for a chat app, you almost never need that. What you do need is fast, reliable per-room message history with cursor-based pagination:

```typescript
const messages = await db
  .select()
  .from(messagesTable)
  .where(lt(messagesTable.createdAt, before))
  .orderBy(desc(messagesTable.createdAt))
  .limit(25);
```

Twenty-five messages per page, loaded when the user scrolls up. The `IntersectionObserver` on a sentinel element triggers the next page load, and scroll position is preserved so the reading flow doesn&apos;t jump.

This dual-database pattern is something I&apos;d use again for any application where data naturally partitions by entity. Room messages belong with the room. User metadata belongs in a shared store.

## Voice calls without a signaling server

Adding voice calling to a chat app usually means standing up a WebRTC signaling server. But when each room is already a stateful server with persistent WebSocket connections to every participant, you already have a signaling channel.

I used Cloudflare RealtimeKit as the SFU (Selective Forwarding Unit) and [partytracks](https://github.com/nickstenning/partytracks) to wrap the SFU protocol with an RxJS-based API. The coordination between peers happens entirely through the existing chat WebSocket.

The flow works like this:

1. User clicks &quot;join call&quot; → client sends `realtimeJoin` over the chat WebSocket
2. The Durable Object broadcasts `realtimeStatus` to all connected clients
3. Each client&apos;s `PartyTracks` instance pushes its audio track to the SFU
4. When a track is ready, the client sends `realtimeUpdate` with the session ID and track name
5. Other clients receive the update and pull the corresponding audio track

```typescript
// Client sends when their audio track is ready
ws.send(JSON.stringify({
  type: &apos;realtimeUpdate&apos;,
  sessionId: session.id,
  trackName: track.name,
}));
```

Without a separate signaling server or SIP, the chat room&apos;s WebSocket connection doubles as the WebRTC signaling channel. The Durable Object coordinates the whole thing because it already knows who&apos;s connected.

### Noise suppression that actually works

Raw microphone audio in a browser sounds terrible. Keyboard typing, fan noise, background chatter. I integrated Jitsi&apos;s RNNoise-based AudioWorklet processor to clean up the audio stream before it hits the SFU:

```typescript
const noiseSuppressedStream = await applyNoiseSuppression(mediaStream);
const audioTrack = noiseSuppressedStream.getAudioTracks()[0];
await partyTracks.push(of(audioTrack));
```

Safari doesn&apos;t support AudioWorklet-based transforms, so it falls back to the browser&apos;s built-in `noiseSuppression`, `echoCancellation`, and `autoGainControl` constraints. Not as effective as RNNoise, but better than nothing.

## Content-addressable image uploads

Image uploads in chat apps usually follow this pattern: upload to server, server stores in object storage, server returns URL. That&apos;s three hops and the server becomes a bottleneck for large images.

I built a content-addressable pipeline that deduplicates images at the storage level:

1. Client converts the image to WebP using the Canvas API (smaller files, consistent format)
2. Client computes SHA-256 hash of the file using `crypto.subtle.digest`
3. Client sends the hash list to the server requesting presigned upload URLs
4. Server checks R2 for existing objects by hash key. If the exact same image already exists, it returns `url: null`. No upload needed.
5. If it&apos;s new, server generates a presigned PUT URL using `aws4fetch` (AWS Signature V4 signing)
6. Client uploads directly to R2

```typescript
// Server checks for duplicates before issuing presigned URLs
const existing = await env.R2.head(hash);
if (existing) {
  return { hash, url: null }; // already stored
}
const presignedUrl = await signRequest({
  method: &apos;PUT&apos;,
  bucket: env.R2_BUCKET,
  key: hash,
});
```

Images are served via `/room/images/:key` with `public, max-age=31536000, immutable` caching headers. Since the key is a content hash, the same image always maps to the same URL, and the CDN caches it forever.

The message itself just stores the JSON array of SHA-256 hashes. The client resolves them to image URLs at render time. This means messages are small even when they contain multiple images.

## User presence with the Idle Detection API

Green/gray online indicators are table stakes for chat apps, but they&apos;re usually wrong. Most implementations just check &quot;is the WebSocket connected?&quot; which means you show as online even if you&apos;ve walked away from your computer.

I used the [Idle Detection API](https://developer.mozilla.org/en-US/docs/Web/API/Idle_Detection_API) (Chrome and Edge only, for now) to detect actual user activity:

```typescript
const controller = new IdleDetector();
controller.addEventListener(&apos;change&apos;, () =&gt; {
  ws.send(JSON.stringify({
    type: &apos;status&apos;,
    userIdleStatus: controller.userState,    // &apos;active&apos; or &apos;idle&apos;
    screenIdleStatus: controller.screenState, // &apos;locked&apos; or &apos;unlocked&apos;
  }));
});
await controller.start({ threshold: 60000 }); // 60 seconds
```

The avatar badge shows green for active, yellow for idle, and gray for locked screen. It&apos;s a small detail, but it makes the presence indicators actually useful instead of just decorative.

## Document Picture-in-Picture for multitasking

The [Document Picture-in-Picture API](https://developer.mozilla.org/en-US/docs/Web/API/Document_Picture-in-Picture_API) lets you pop out an entire web page into a floating window, not just a video element. I used it to let users keep the chat room visible while working in other tabs:

```typescript
const pipWindow = await documentPictureInPicture.requestWindow({
  width: 400,
  height: 600,
});
// Copy styles into the PiP window
for (const sheet of document.styleSheets) {
  const style = document.createElement(&apos;style&apos;);
  style.textContent = Array.from(sheet.cssRules)
    .map(rule =&gt; rule.cssText)
    .join(&apos;&apos;);
  pipWindow.document.head.appendChild(style);
}
// Render React components into the PiP window
const root = createRoot(pipWindow.document.body);
root.render(&lt;Room {...props} /&gt;);
```

This is still experimental (Chrome-only), but it&apos;s a killer feature for a chat app. You get a full-featured floating chat window, not a stripped-down mini view.

## The frontend stack

The client is React 19 with the React Compiler enabled via `babel-plugin-react-compiler`. This gives automatic memoization without manually wrapping components in `useMemo` and `useCallback`. It&apos;s one less thing to think about during development, and the performance is consistently good.

State management uses TanStack Query v5 for server state (with infinite queries for paginated room lists) and `ahooks/useWebSocket` for the real-time connection with automatic reconnection. The `ky` HTTP client handles auth redirects transparently.

Styling is Tailwind CSS 4 with Shadcn-style component primitives. The whole thing builds with Vite 8 and deploys as a static site.

## What I&apos;d do differently

**Cross-room search is hard.** Because messages live in per-room Durable SQLite instances, there&apos;s no single database to run a full-text search across. If I needed global search, I&apos;d have to either query each room individually (expensive) or maintain a separate search index (complex). For now, per-room history search is sufficient.

**WebSocket state recovery is subtle.** When a Durable Object hibernates and wakes up, the WebSocket connections survive but you need to carefully rehydrate your session state from `deserializeAttachment()`. I spent more time debugging this than I expected. The documentation exists but the edge cases are under-documented.

**The partytracks patch.** I had to patch `partytracks@0.0.55` to add `credentials: &apos;include&apos;` to its internal fetch calls. The library doesn&apos;t send cookies by default, which breaks authentication when proxying through your own server. Bun&apos;s patch system made this easy, but it&apos;s a dependency wart.

**Browser compatibility is a real constraint.** Idle Detection, Document PiP, and AudioWorklet noise suppression are all Chrome-only. Safari and Firefox users get a degraded experience. This is the cost of living on the bleeding edge.

## The stack, summarized

| Layer | Technology |
|-------|-----------|
| Compute | Cloudflare Workers + Hono |
| Room state | Durable Objects with Durable SQLite |
| Global data | Cloudflare D1 |
| File storage | Cloudflare R2 |
| Voice | Cloudflare RealtimeKit (SFU) |
| Auth | better-auth with OAuth PKCE |
| Frontend | React 19, React Compiler, Vite 8, Tailwind CSS 4 |
| ORM | Drizzle (for both D1 and Durable SQLite) |

Everything runs on Cloudflare&apos;s edge network with smart placement enabled, so the Worker is automatically co-located near the D1 database and Durable Objects it talks to.

## Try it or build your own

The source code is at [github.com/Jazee6/web-chat](https://github.com/Jazee6/web-chat). The live demo runs at [chat.jaze.top](https://chat.jaze.top).

If you&apos;re building real-time features and considering Cloudflare&apos;s platform, the key insight is this: Durable Objects aren&apos;t just key-value stores with a WebSocket. They&apos;re genuinely stateful servers that can hold complex data structures, run embedded databases, and coordinate multi-party real-time sessions. The mental model shift from &quot;stateless API + external state&quot; to &quot;the room is the server&quot; takes some getting used to, but once it clicks, it simplifies everything.</content:encoded></item><item><title>Cloudflare AI Web 4.0</title><link>https://jaze.top/posts/cloudflare-ai-web-40/</link><guid isPermaLink="true">https://jaze.top/posts/cloudflare-ai-web-40/</guid><description>Cloudflare AI Web 4.0正式推出</description><pubDate>Tue, 30 Sep 2025 00:00:00 GMT</pubDate><content:encoded>![banner](https://blog-cdn.jaze.top/2025/09/71724a1cf8d68bb6701a938ff9cc38a9)

Cloudflare AI Web 4.0 正式发布。这是一次从底层到体验的「完全重构」：项目从 Nuxt 迁移到 Next.js 15，配合全新的 AI SDK
流式能力与本地优先的数据存储，让部署更简单、体验更顺滑、扩展更轻松。

- Demo：https://ai.jaze.top
- 源码：https://github.com/Jazee6/cloudflare-ai-web

## 特性

- 技术栈全面升级：Next.js 15 + React 19 + shadcn/ui + Biome。
- 聊天体验重做：流式输出、Reasoning 折叠可视化、模型一键切换、停止/重试控制。
- 本地优先：会话与消息使用 IndexedDB 存储，隐私友好、离线可读。
- 开箱即用的 Serverless API 与轻量鉴权（APP_PASSWORD）。
- Vercel 一键部署 &amp; 官方 Docker 镜像。

## 核心功能与亮点

### 更强的流式聊天体验

- 基于 AI SDK（ai / @ai-sdk/react）实现流式消息与 UI 同步。
- 内置 Reasoning 流提取（extractReasoningMiddleware），推理片段以 &lt;think&gt; 标签传输，在前端以「Reasoning」折叠区展示（支持边流边看、加载指示）。
- 支持停止/重试控制、提交状态提示（Generating…）、智能滚动与回到底部浮标。
- 为控制上下文长度，仅发送最近 10 条消息参与推理。

### 现代化 UI/UX

- 采用 Shadcn UI 与 Tailwind v4，内置 Sidebar 布局、明暗主题（next-themes）。
- 动效增强：TextEffect、TextShimmer、ViewTransition（React 19 实验特性）。
- 用封装Remark的StreamDown库渲染消息，支持代码高亮、数学公式（KaTeX）、Mermaid 图表。

## Roadmap

-[x] 图像生成页面。
-[x] 工具调用与多模态能力增强。
-[x] Gemini AI Gateway支持。

（由GPT-5辅助生成）</content:encoded></item><item><title>Easy Auth</title><link>https://jaze.top/posts/easy-auth/</link><guid isPermaLink="true">https://jaze.top/posts/easy-auth/</guid><description>Easy Auth 使用指南</description><pubDate>Wed, 07 Jan 2026 00:00:00 GMT</pubDate><content:encoded>![banner](https://blog-cdn.jaze.top/2026/01/a698368ee0accb0d8928b028990020a3.avif)

## 简介

Easy Auth 是基于Better Auth的OIDC &amp; SSO provider，开源可自行部署，类似于Auth0、Okta等商业化身份认证服务。

- 地址：https://account.jaze.top
- 仓库：https://github.com/Jazee6/easy-auth

多个 Web 应用和 小程序 可以使用 Easy Auth 作为统一的身份认证服务，用户只需注册一个账号即可登录所有接入的应用。

## 使用

按`README`部署并设置为管理员

进入Apps页面，创建一个新的应用

![](https://blog-cdn.jaze.top/2026/01/90b3ae9b5f9ccd180674200b5abe3b41.avif)

可以填写多个回调地址，用于不同环境。请注意回调地址需要完全匹配

![](https://blog-cdn.jaze.top/2026/01/91087b97dbf162f923e7996f53290f83.avif)

在Client App配置`Better Auth`的`genericOAuth`插件

```typescript
export const authConfig: BetterAuthOptions = {
    database: drizzleAdapter(env.web_chat, {
        provider: &quot;sqlite&quot;,
    }),
    plugins: [
        genericOAuth({
            config: [
                {
                    providerId: &quot;easy-auth&quot;,
                    clientId: process.env.EASY_AUTH_CLIENT_ID,
                    clientSecret: process.env.EASY_AUTH_CLIENT_SECRET,
                    discoveryUrl:
                        process.env.NODE_ENV === &quot;production&quot;
                            ? &quot;https://account.jaze.top/api/auth/.well-known/openid-configuration&quot;
                            : &quot;http://localhost:3000/api/auth/.well-known/openid-configuration&quot;,
                    pkce: true,
                    scopes: [&quot;openid&quot;, &quot;profile&quot;, &quot;email&quot;, &quot;offline_access&quot;],
                    overrideUserInfo: true,
                },
            ],
        }),
    ],
    trustedOrigins: [process.env.SITE_URL],
    session: {
        cookieCache: {
            enabled: true,
            maxAge: 5 * 60,
        },
    },
    advanced: {
        cookiePrefix: &quot;wc&quot;,
    },
};
```

支持[Stateless Mode](https://www.better-auth.com/docs/concepts/session-management#stateless-session-management)
，无需数据库存储session，你可以添加以下配置固定User ID

```typescript
export const authConfig: BetterAuthOptions = {
    // database: drizzleAdapter(env.web_chat, {
    //     provider: &quot;sqlite&quot;,
    // }),
    databaseHooks: {
        user: {
            create: {
                // @ts-ignore
                before: (user) =&gt; {
                    return {
                        data: {
                            ...user,
                            id: user.sub,
                        },
                    };
                },
            },
        },
    },
};
```

支持[SSO](https://www.better-auth.com/docs/plugins/sso)插件，回调地址填写`/api/auth/sso/callback/easy-auth`

完整示例应用仓库：[Web Chat](https://github.com/Jazee6/web-chat)，使用Cloudflare生态构建的多功能聊天应用</content:encoded></item><item><title>Meta Quest 2 VR 简评+指南</title><link>https://jaze.top/posts/meta-quest2/</link><guid isPermaLink="true">https://jaze.top/posts/meta-quest2/</guid><description>Meta Quest 2 VR 简评+指南</description><pubDate>Tue, 15 Feb 2022 00:00:00 GMT</pubDate><content:encoded>## 故事

为什么买Quest 2？去年暑假，我的选择有

* Nintendo Switch
* PlayStation 4
* Xbox Series S
* Pico Neo 3
* Oculus Quest 2（已更名Meta Quest 2 Facebook:No-&gt;Meta:Yes!）

Nintendo Switch？创意不错，能带着走，还能连接TV。不过17年的产品了，说不定快出次世代的机子了（结果出了个OLED版本）。PlayStation
4？Xbox Series S？挺常见的形态了，没什么新意，估计会吃灰。偶然发现了VR这个玩意还算“成熟”了，对比Pico Neo 3和Oculus Quest
2后，还是选择了更难折腾的Quest 2。64G版本全新美亚2.1k到手，DHL物流23天。目前统一升级为128G起步。

基本参数：

* 单眼分辨率1832\*1920
* RGB排列LCD
* 120Hz
* 视场角（FOV）约90°
* 估算像素密度（PPD）21.9
* 硬瞳距（IPD）58/63/68mm
* 内置定位6-DoF
* 503g
* 高通骁龙XR2
* 6+64G
* 3640mAh
* 802.11 AX

## 开箱

![](https://blog-cdn.jaze.top/2024/01/c037b566d77093498b26cdd42dfd0567.webp)

就剩这图了

## 外观

![](https://blog-cdn.jaze.top/2024/01/662f4075c7e940799a48a97e284a9307.webp)

含4颗黑白摄像头

![](https://blog-cdn.jaze.top/2024/01/8c472ebd2503692415c64ba8f74303f9.webp)

熟悉的ABXY

![](https://blog-cdn.jaze.top/2024/01/9c1ac5ed564943a358857e00563a7526.webp)

侧边键 模拟抓握

## 体验

### 佩戴

舒适度确实一般，在能接收的范围内，不过可以购买第三方头戴来提升舒适度，100RMB左右。隔壁Neo 3的电池后置就挺不错，平衡了部分重量，不会那么压头。

可以佩戴眼镜游玩，眼镜放不进去的话还能套一个附赠的眼镜支架，TB上也有定制读书的磁吸镜片，同样是100RMB左右。

![](https://blog-cdn.jaze.top/2024/01/5db0a5a3726b89afcf4023ea8ff8b93f.webp)

鼻子部分的凹槽没有针对国人设计，鼻子不够高还是会轻微漏光。

### 游戏

120Hz的Beat Saber还是很爽的，还有Half-Life: Alyx也让人身临其境。其他的不好说，这两应该是代表。

像一些运动幅度较大的游戏，如过山车/赛车等，会产生类似晕车般的感受，毕竟人眼和身体接收的信息不同，眼睛看到的是你在动，但是身体感受没有动，着实容易头晕。

内置游戏内录 _Spice and Wolf VR_

音质：扬声器虽然小，但音质意外的不错，能感觉到空气在被推动（物理）

![](https://blog-cdn.jaze.top/2024/01/df8abfa07c427e78f28e9d2d698289f0.webp)

_Unplugged VR_

支持手势识别，即不用手柄也能操控设备，畅玩部分游戏。如上面这款_Unplugged_。

![](https://blog-cdn.jaze.top/2024/01/2fa3255848fe9b444d060cb65e49d51a.webp)

_Apollo 11 VR_ 对接

![](https://blog-cdn.jaze.top/2024/01/dbacde0150fc45b33606d2a8dec103a8.webp)

_Takagi san VR_ 互动

### 视频

Skybox VR还不错，支持串流/局域网/本地，看普通视频也带劲（大屏+类似空间音频）。

![](https://blog-cdn.jaze.top/2024/01/8b567097095baf3089ba6863742413f3.webp)

_Sono Bisque Doll wa Koi o Suru_

![](https://blog-cdn.jaze.top/2024/01/398670feb286f28636d96e0f010a8dd1.webp)

场景选择

### 槽点

* 刚开始游玩的时候确实很惊艳，但没多久还是发现了视场角(FOV)不太够的问题，当然，如果你足够专注的话，还是不会注意到画面周围的黑边。
* 分辨率还差点意思，其实已经很高了，但还是无法满足近距离苛刻的人眼。

* * *

## 指南

主要阻碍是Meta不在中国大陆提供服务。下面给出我的解决方案。

### 1.激活

主要思路就是让Quest 2能够科学上网，连接能刷机的路由器是最简单直接的。

或者开启电脑/手机网关模式，连接WIFI时设置代理即可。

还有一种第三方软件支持激活：[https://ochelper.xlemon.cn/home.html](https://ochelper.xlemon.cn/home.html)

### 2.安装应用

这里指的是安装第三方内置应用（有能力还是要去商店支持正版）。

思路：申请开发者权限[https://www.yuque.com/oculus-helper/start/developer](https://www.yuque.com/oculus-helper/start/developer)\-&gt;
打开开发者模式-&gt;连接电脑-&gt;ADB Connect 或者使用 SideQuest安装

![](https://blog-cdn.jaze.top/2024/01/4881dbced3c215d333b56ad230744d82.webp)

[https://sidequestvr.com/](https://sidequestvr.com/)

### 3.投屏

安卓电视：推荐CastReceiver [http://www.downcc.com/soft/529102.html](http://www.downcc.com/soft/529102.html)

PC：可以使用Oculus官方的网页投屏 [https://www.oculus.com/casting](https://www.oculus.com/casting) 还有ADB
scrcpy或者SideQuest均可

![](https://blog-cdn.jaze.top/2024/01/9215516a4ca2f9e38148f1ff38fbc83f.webp)

Web Cast

手机：使用Oculus应用

### 4.串流

* oculus官方的Airlink，简单直接，无线有线均可。
* 开源的ALVR：[https://github.com/alvr-org/ALVR](https://github.com/alvr-org/ALVR)，无线。
* Virtual Desktop：需购买，可无线。

### 5.资源

主要是Oculus商店和Steam VR商店（有能力一定要支持正版）

其他：

* [https://gizvr.cn/resources/?order=rating\_weighted](https://gizvr.cn/resources/?order=rating_weighted) （极客VR论坛
  Free 资源较全）
* [https://questbx.com/](https://questbx.com/) （快斯特宝箱 下载安装方便 需要流量费 资源较全 目前好像嗝屁）
* [http://share.wgzeyu.vip/](http://share.wgzeyu.vip/) （Beat Saber资源大全）
* [https://www.baozouvr.com/](https://www.baozouvr.com/) （暴走VR Free 百度网盘）

另外一些付费资源网需自辨。

### 6.民间文档

* [https://www.yuque.com/oculus-helper/start/app](https://www.yuque.com/oculus-helper/start/app)
* [https://ocguide.eyw015.com/](https://ocguide.eyw015.com/)
* [https://bs.wgzeyu.com/](https://bs.wgzeyu.com/) （Beat Saber）
* [https://vrar123.com/](https://vrar123.com/) （导航类）

### 7.其他

把人录进去可以使用[https://www.liv.tv/](https://www.liv.tv/) ，摄像头＋绿幕

可以安装Clash更新系统，部分流量不能代理。

![](https://blog-cdn.jaze.top/2024/01/4259beec6a1041a8ebaa200a6e77fe3f.webp)

## 总结

作为入门级别VR设备来说确实很值得购买，它支持有线/无线串流Steam VR，或是在其内部空间直接安装游戏。可能小小的阻碍就是科学上网了吧。

![](https://blog-cdn.jaze.top/2024/01/87c6fae4b4b0f07ce32a1a967f21134f.webp)

美亚128

![](https://blog-cdn.jaze.top/2024/01/02370a99bcfac6f95ce84c89d6bb4b54.webp)

日亚128

## 后记

![](https://blog-cdn.jaze.top/2024/01/24f922310fa9d5f71116320da09be84b.webp)

离Meta还有多远？</content:encoded></item><item><title>腕上饭卡—手表模拟全加密卡</title><link>https://jaze.top/posts/wear-card/</link><guid isPermaLink="true">https://jaze.top/posts/wear-card/</guid><description>适用于能开通空白门卡的，带NFC功能的手表/手环模拟门卡/饭卡</description><pubDate>Fri, 12 Feb 2021 00:00:00 GMT</pubDate><content:encoded>适用于能开通**空白门卡**的，带**NFC**功能的手表/手环模拟门卡/饭卡

本教程适用于能开通**空白门卡**的，带**NFC**功能的手表/手环模拟门卡/饭卡

先上成品图（以**小米手表**为例）

![](https://blog-cdn.jaze.top/2023/11/4e6e2d22b8dc64ad333724ae7c413c7f.webp)

在开始模拟**饭卡**前，请确认以下几点，否则可能会导致模拟/刷卡失败

* 确保为**联网**卡，即卡中的金额等数据储存在云端，使用手表刷卡时，实体卡中的金额也会**同步**变化。（被请喝茶了**概不负责
  ** :) ）
* 确保读卡器不会读取卡片的**0扇区**（含厂商信息），因为大部分卡片模拟无法写入
* 可以先用**Mifare**读取下卡片的标签信息，如果是SAK08基本可以模拟（国产半加密卡几乎无解，详见下方**补充**）

## 准备

1.**PN532**套件，淘宝价格30元左右，需要USB转TTL，记得让卖家**焊接**好

![](https://blog-cdn.jaze.top/2023/11/05a4ce64ed6c26bfd188633c913ec4bc.webp)

2.Windows平台电脑（此处仅提供windows平台套件）

3.全功能NFC手机一台（以**小米8**为例），安装好[Mifare Classic Tool](https://www.icaria.de/mct/releases/)（建议最新版本）

4.驱动程序 下载地址：[https://jmzzz.lanzout.com/iiQ7Alkh6ba](https://jmzzz.lanzout.com/iiQ7Alkh6ba) （2022/01/24更新）

5.操作程序 下载地址：[https://jmzzz.lanzout.com/iLYV6lkxi9g](https://jmzzz.lanzout.com/iLYV6lkxi9g)（2022/01/24更新）

## 开始破解

1.安装好驱动后，将PN532模块连接到PC，打开**设备管理器**，记录下**端口**

![](https://blog-cdn.jaze.top/2023/11/989c94594a793c1fda19b1a7ffd18700.webp)

2.用**记事本**打开532破解全加密\\破解\\libnfc.conf，修改成对应的**端口**

![](https://blog-cdn.jaze.top/2023/11/96c7eb8fa6bbdd88bc94a5a1eb98e60d.webp)

3.将饭卡放置在PN532模块上，打开破解内的.exe程序，点击**全加密卡破解密钥**，直到连续出现**3次**及以上的密钥（Key
A）并复制下来，并关闭CMD窗口

![](https://blog-cdn.jaze.top/2023/11/a07ca19d6995f456320c575587d2f9a1.webp)

![](https://blog-cdn.jaze.top/2023/11/b165ba29b3cbfd5b1e63a3b978023d36.webp)

4.将Key A使用已知密钥读取，跑Key B（大概几分钟就能搞定）

![](https://blog-cdn.jaze.top/2023/11/d459c6deb59aef32e1d58ea69e332167.webp)

成功之后会在该目录下生成**key.dump**

![](https://blog-cdn.jaze.top/2023/11/adee036bcff76041d7729487e41a054e.webp)

## 提取密钥

手表无法直接模拟全加密的饭卡，因此还需要提取饭卡的密钥

1.打开532破解全加密\\数据\\Dump2Text.exe，将刚刚得到的key.dump转换为.txt文本

![](https://blog-cdn.jaze.top/2023/11/0863d5238bfb0a2b0f844cc8f638ebf3.webp)

2.打开key.dump.txt，将里面的16组密钥复制出来到另一个新建的.txt文档（全为F的可以省略）

![](https://blog-cdn.jaze.top/2023/11/c0640564ea47db6ac2ed06fd01beb7f0.webp)

## 模拟饭卡

1.手机上打开打开NFC功能，打开[Mifare Classic Tool](https://www.icaria.de/mct/releases/)
，选择编辑/新建密钥文件→新建文件→将密钥写入→保存（如果不能一次性写入的话，可以一行一行写入）

![](https://blog-cdn.jaze.top/2023/11/ff1b6a78e818a49aa3e867d99c614289.webp)

全部写入之后保存

2.选择读标签→将你刚刚的密钥勾上→将饭卡放在手机NFC的读取区域→开始映射及读取标签

![](https://blog-cdn.jaze.top/2023/11/c342fe60d035bd17c682875d4c4df4e5.webp)

3.将读取出来的数据保存（所有扇区都能读取出来才算成功哦）

![](https://blog-cdn.jaze.top/2023/11/d85bcdd3372406f05e06accb22551810.webp)

4.打开**小米穿戴**→我的→卡包→添加新门卡→空白门卡

![](https://blog-cdn.jaze.top/2023/11/b3ae09e12cfe2761867a9ba61e345432.webp)

5.回到Mifare，选择写标签→选择转储→选所有扇区→选刚刚你保存的转储文件→将手表放在手机NFC读取区域→写入

![](https://blog-cdn.jaze.top/2023/11/f26054a0df6965e512ae111b966691bc.webp)

![](https://blog-cdn.jaze.top/2023/11/7c5d37aec03a0d17dcd173f7168c7670.webp)

## 完成

接下来你就能拿着你的手环/手表去食堂/商店刷卡了ヾ(•ω•\`)o

亲测在食堂/商店/热水处可以使用息屏支付，且不会被查水表，充值时可以使用网上充值和原饭卡现金充值

## 排障

如果遇到该驱动问题，可以右键-属性-驱动程序-更新驱动程序-浏览我的电脑以查找驱动程序-从列表中获取-选2008年的安装即可

![](https://blog-cdn.jaze.top/2023/11/da2c4eaa77abf35152b3f16e2758c5d3.webp)

![](https://blog-cdn.jaze.top/2023/11/831060f3503afd7b2375c8cfe97300de.webp)

## 补充（21.10.2）

关于**国产半加密卡**，因为没有漏洞，用pn532的话只能暴力解卡，费时费力，只能碰碰运气

![](https://blog-cdn.jaze.top/2023/11/bcd588562353adaabc7bba59378e11e1.webp)

使用默认密钥读取结果

![](https://blog-cdn.jaze.top/2023/11/c3413886b68bc7472195d0d44ae8b0e6.webp)

全加密破解时卡住

## 结尾

部分内容参考自酷安@赵s哥等人的动态，感谢

驱动及操作程序来自网络，仅供交流学习使用

本教程还有存在有不完善的地方，欢迎在评论区补充</content:encoded></item></channel></rss>